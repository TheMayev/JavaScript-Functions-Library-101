// Even Numbers 

// function that takes array as parameter and return array of even numbers

const evenNum = (arr) => {
  let even = y.filter(el => {
    return el % 2 === 0;
  })
  return even;
};


